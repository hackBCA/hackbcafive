import LazyIcon from './LazyIcon';
import { faChevronRight } from '@fortawesome/free-solid-svg-icons';

export default function ChevronRight() {
    return (
        <LazyIcon icon={faChevronRight}></LazyIcon>
    );
}