import LazyIcon from './LazyIcon';
import { faEnvelopeOpen } from '@fortawesome/free-solid-svg-icons';

export default function Email() {
    return (
        <LazyIcon icon={faEnvelopeOpen}></LazyIcon>
    );
}