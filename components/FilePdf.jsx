import LazyIcon from './LazyIcon';
import { faFilePdf } from '@fortawesome/free-solid-svg-icons';

export default function FilePdf() {
    return (
        <LazyIcon icon={faFilePdf}></LazyIcon>
    );
}